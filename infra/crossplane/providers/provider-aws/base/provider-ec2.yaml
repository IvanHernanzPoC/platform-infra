apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-aws-ec2
spec:
  package: xpkg.upbound.io/upbound/provider-aws-ec2:v2.0.2
  # aplicar si hay mezcla de registry de providers
  # runtimeConfigRef:
  #   name: provider-aws-s3-rc   # puedes reutilizar el mismo SA/DRC
